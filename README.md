# 富文本转化工具

一个本地运行的富文本转化网页工具，支持文本上色、序号拼接、富文本预览、配置导入导出、数值自动上色和富文本反向解析。

## 使用方式

直接用浏览器打开 `index.html`。

## 文件说明

- `index.html`：页面结构
- `styles.css`：页面样式
- `app.js`：工具逻辑
- `Text.xlsx`：示例表格

## 注意

工具完全在浏览器本地运行，不调用接口，不消耗 token。
